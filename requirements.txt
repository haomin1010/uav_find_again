# Minimal Python dependencies for the non-ROS algorithm/demo utilities.
#
# Keep ROS2 packages out of pip:
#   rclpy, geometry_msgs, std_msgs, launch_ros, ros_gz/ros_ign
# must come from apt /opt/ros/humble, not pip.
#
# Recommended environment:
#   conda create -y -n uav_gazebo python=3.10
#   conda activate uav_gazebo
#   pip install -r requirements.txt

numpy==1.26.4
matplotlib==3.8.2
opencv-python-headless==4.8.1.78
